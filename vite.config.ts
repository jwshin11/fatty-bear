import { defineConfig } from 'vite'

export default defineConfig({
  // Relative asset URLs so the build works from any host or subpath
  // (GitHub Pages project sites, Netlify, opening dist/index.html directly).
  base: './',
})
